# 학습 데이터셋 정의 (spec 3.3, 3.4). Ultralytics 형식.
# 변환 스크립트(scripts/convert_*.py)가 datasets/ywhob 아래에 images/, labels/를 만든다.

path: datasets/ywhob
train: images/train
val: images/val
test: images/test

names:
  0: person   # 가려진 부분까지 포함한 전체 박스 (CrowdHuman full box)
  1: head
