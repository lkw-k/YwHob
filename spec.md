# YwHob 기술 명세서 (spec.md)

README.md의 설계를 기준으로 **모델 학습부터 서비스 운영까지**의 구현 명세를 정리한다.

| 항목 | 내용 |
|---|---|
| 문서 상태 | 초안 (검토 대기) |
| 기준 문서 | README.md (c400295) |
| 작성일 | 2026-09-27 |

> 표기 규칙
> - **(확정)**: README 또는 결정 사항으로 확정된 내용
> - **(잠정)**: 구현 시작을 위한 초기값. 실측 후 조정
> - **(수정 예정)**: 방향만 정해졌고 세부 방식이 바뀔 수 있음

---

## 1. 개요

### 1.1 목적

박람회(예상 1만 명) CCTV를 30초마다 캡처해 구역별 혼잡도를 산출하고, 수치와 블러 이미지를 웹 서버로 전달한다.

### 1.2 담당 범위

```
[모델 학습] 데이터 준비 → 라벨 변환 → 파인튜닝 → 평가 → 최적화(ONNX/양자화)
[서비스]   영상 수신 → 캡처 → 사람 검출(+머리 확인) → 혼잡도 산출 → 블러 → Redis 전달
```

웹 서버 화면과 서버 측 이미지 보관은 담당 범위가 아니다. 인터페이스(9장)만 합의한다.

### 1.3 확정 사항

| 항목 | 내용 |
|---|---|
| 모델 | **YOLO26 Small (`yolo26s`)** (확정) |
| 검출 방식 | 사람 박스 + 머리 확인 (수정 예정) |
| 실행 환경 | 노트북 1대. 우선 1대 기준, 추후 변경 가능 |
| 측정 주기 | 30초 |
| 샘플링 | 주기당 3초간 5장 |
| 출력 | 혼잡 3단계(0/1/2) + 연속 점수 + 블러 이미지 |
| 전달 | Redis에 최신 값 덮어쓰기 |

---

## 2. 전체 구성

```
[노트북 1대]
CCTV ×N ─RTSP─▶ Capture ─▶ Detect ─▶ Aggregate ─▶ Publish ─▶ Redis ─▶ 웹 서버
                (시점 분산)  (배치 추론)  (중앙값/EMA/     (JSON+이미지)
                   │        (사람+머리)   히스테리시스)       ▲
                   └──── 원본(메모리) ─▶ Blur ─────────────┘
```

- 단계 간에는 큐로 연결하고, 캡처, 추론, 블러, 전송은 비동기로 분리한다.
- 원본 프레임은 디스크에 쓰지 않는다.

---

## 3. 모델 학습

### 3.1 모델

| 항목 | 내용 |
|---|---|
| 베이스 | `yolo26s.pt` (COCO 사전학습) |
| 프레임워크 | Ultralytics (YOLO26을 지원하는 버전으로 고정, `requirements.txt`에 명시) |
| 선정 이유 | 경량(small)이고 NMS가 없는 end-to-end 구조라 CPU나 엣지 추론과 ONNX 변환에 유리함 |
| 학습 환경 | 미정. 추론용 노트북과 별개로 GPU 환경 필요 (14장) |

### 3.2 검출 클래스 (수정 예정)

| id | 클래스 | 정의 |
|---|---|---|
| 0 | `person` | 사람 전체 박스. 가려진 부분까지 포함해 **크게** 잡는다 |
| 1 | `head` | 머리 박스 (얼굴 방향 무관, 뒷머리 포함) |

사람 박스만 쓰면 멀리 있는 작은 물체가 오탐되고, 머리만 쓰면 원거리 누락과 공 같은 물체 오탐이 생긴다. 그래서 두 클래스를 같이 검출하고 매칭 규칙(5.3)으로 사람 여부를 확정한다.

### 3.3 데이터셋

| 데이터 | 사용 라벨 | 용도 | 주의점 |
|---|---|---|---|
| CrowdHuman | full box → `person`, head box → `head` | 주 학습 데이터 | `ignore`/mask 영역은 제외 처리. full box는 이미지 밖으로 나갈 수 있어 경계에서 자름 |
| SCUT-HEAD | head → `head` | 머리 검출 보강 (교실 등 고각 시점) | `person` 라벨이 없어 **라벨 누락** 문제가 생김 (아래 참고) |
| 현장 샘플 | `person`, `head` 직접 라벨링 | 도메인 적응, 최종 평가 | 학습용이라 원본 보관이 필요함. 촬영 동의, 접근 제한, 행사 후 삭제 시점을 주최 측과 협의 |
| 오탐 보강 (선택) | 라벨 없음 (배경) | 공, 풍선, 마네킹, 포스터 인물 등의 오탐 억제 | 현장 부스 구성 확인 후 수집 |

**SCUT-HEAD 라벨 누락 처리 (잠정)**
- 1차: CrowdHuman과 현장 샘플만으로 학습하고 SCUT-HEAD는 제외한다.
- 2차: 1차 모델로 SCUT-HEAD에 `person` 의사 라벨을 붙여 합친 뒤 재학습하고, 성능이 오르는지 비교한다.

**라이선스**: CrowdHuman과 SCUT-HEAD는 연구 목적 사용 조건이 붙어 있다. 행사 서비스에 써도 되는지 사용 전에 확인한다 (14장).

### 3.4 데이터 분할

| 세트 | 구성 | 비고 |
|---|---|---|
| train | CrowdHuman train + 현장 샘플 70% | |
| val | CrowdHuman val + 현장 샘플 15% | 하이퍼파라미터와 체크포인트 선택 |
| test | 현장 샘플 15% | **학습에 절대 사용하지 않음.** 최종 판정용 |

현장 샘플은 같은 카메라의 연속 프레임이 train과 test에 섞이지 않도록 **촬영 세션 단위**로 나눈다.

### 3.5 학습 설정 (잠정)

```yaml
model: yolo26s.pt
data: configs/data.yaml
imgsz: 1280          # 원거리 소형 객체 대응. 960과 비교 실험
epochs: 100
batch: auto
patience: 20
optimizer: auto
augment:
  mosaic: 1.0
  scale: 0.5
  fliplr: 0.5
  flipud: 0.0        # 상하 반전 금지 (CCTV 시점 고정)
  hsv_v: 0.4         # 실내 조명 변화
```

- 실험은 `imgsz`(960/1280)와 SCUT-HEAD 포함 여부의 조합으로 진행한다.
- 실험 결과는 `runs/` 아래에 두고, 선정 모델은 `models/`에 버전을 붙여 보관한다.

### 3.6 평가 지표

| 구분 | 지표 | 목표 (잠정) |
|---|---|---|
| 검출 | `person`, `head` 각 mAP50 | 기준선 측정 후 설정 |
| 검출 | 원거리(`head` 박스 높이 16px 미만) recall | 기준선 측정 후 설정 |
| **인원 추정** | 구역별 인원 MAE / MAPE (현장 test) | MAPE ≤ 20% |
| 오탐 | 사람 없는 프레임에서 확정된 사람 수 | 0에 수렴 |
| 속도 | 1장당 추론 시간 (노트북, 최적화 후) | 6장에서 산출 |

서비스 품질은 mAP보다 **인원 추정 오차**에 더 직접적으로 달려 있으므로, 모델 채택은 인원 MAE/MAPE를 기준으로 한다.

### 3.7 최소 검출 크기 실측

README의 선행 과제다. 원거리 누락이 "사람 없음"으로 집계되는 것을 막는다.

1. 현장 샘플 test에서 GT `head` 박스 높이(px)별 **확정 사람**(5.3 규칙 적용 후) recall 곡선을 구한다. 머리가 검출돼야 사람으로 확정되므로 기준은 머리 크기다.
2. recall이 기준치(잠정 0.7) 아래로 떨어지는 머리 높이를 **최소 검출 크기**로 정한다.
3. 카메라별로 이 크기보다 작게 보이는 영역을 ROI 마스킹 제외 영역으로 지정한다 (7장).

---

## 4. 모델 최적화와 배포

### 4.1 변환

| 단계 | 방법 |
|---|---|
| ONNX 변환 | `yolo export format=onnx imgsz=<선정값> dynamic=True` (배치 5 이상 지원) |
| 양자화 | INT8, 현장 샘플 100~300장으로 캘리브레이션 |
| 런타임 | 노트북 사양에 따라 결정 (아래) |

| 노트북 GPU | 런타임 (잠정) |
|---|---|
| NVIDIA | TensorRT 또는 ONNX Runtime CUDA EP |
| Intel 내장 | OpenVINO |
| 없음 | ONNX Runtime CPU EP |

### 4.2 채택 조건

- 양자화 후 인원 MAPE 악화가 FP32 대비 **+2%p 이내**여야 한다. 넘으면 FP16이나 FP32를 쓴다.
- 결과물: `models/yolo26s-ywhob-v{N}.onnx` 및 모델 카드(`models/README.md`: 데이터, 설정, 지표 기록)

### 4.3 수용 카메라 수 산출

```
T_cam   = 5장 배치 추론 시간 + 블러 + 전송
가용    = 30초 - 3초(캡처) - 여유 5초 = 22초
최대 카메라 수 = floor(22초 / T_cam)
```

벤치마크 스크립트(`scripts/benchmark.py`)로 해상도별 `T_cam`을 측정하고 결과를 README 미결 사항에 반영한다.

**영상 디코딩 부하**: 위 식은 추론 시간만 본다. RTSP를 계속 연결해 두면 캡처하지 않는 27초 동안에도 N개 스트림을 계속 디코딩하므로, 카메라 수가 늘면 추론보다 디코딩이 먼저 한계에 닿을 수 있다. 벤치마크에는 N개 스트림을 동시에 디코딩하는 부하를 반드시 포함하고, 부족하면 5.1의 완화책을 적용한다.

---

## 5. 추론 파이프라인

### 5.1 캡처

| 항목 | 설계 |
|---|---|
| 입력 | RTSP (카메라 사양 확정 후 변경 가능) |
| 샘플링 | 3초 동안 5장, 0.75초 간격 (0, 0.75, 1.5, 2.25, 3.0초) |
| stagger | 카메라 i의 캡처 시작 = `i × (30초 / N)` 오프셋 |
| 연결 | 스트림을 상시 연결하고 최신 프레임만 유지 (버퍼 누적 방지) |
| 디코딩 부하 완화 | 카메라 설정에서 fps를 낮추거나(예: 5fps) 서브스트림 사용. 그래도 부족하면 캡처 구간에만 연결하거나 카메라 스냅샷 API 사용을 검토 |
| 원본 | 메모리에만 유지하고 처리가 끝나면 즉시 폐기 |
| 장애 | 5장 중 3장 미만이면 해당 주기를 `error`로 처리 |

### 5.2 검출

- 5장을 한 배치로 추론한다.
- 출력: 프레임별 `person` 박스와 `head` 박스 목록
- 신뢰도 임계값(잠정): `person` 0.35, `head` 0.30
- `max_det`(프레임당 최대 검출 수)를 1000으로 올린다. 기본값 300은 혼잡한 장면에서 사람과 머리 박스를 합치면 넘을 수 있고, 넘으면 인원이 조용히 잘린다.

### 5.3 사람 확정 규칙 (수정 예정)

```
후보 조건 (person P, head H):
  1) H의 중심점이 P의 상단 40% 영역 안에 있다        (top_ratio = 0.4)
  2) H의 너비가 P 너비의 15%~70% 사이다              (크기 비율 검사)

매칭 (1:1):
  person을 신뢰도 높은 순으로 처리
    → 후보 H 중 아직 안 쓴 것, 기대 위치(P 상단 중앙)에 가장 가까운 H를 배정
  배정된 P = "확정 사람"으로 카운트

미배정 person → 카운트하지 않음 (미확정 person으로 로그 기록)
미배정 head   → 카운트하지 않음 (미확정 head로 로그 기록)
```

- 크기 비율 검사는 멀리 있는 작은 머리가 가까운 큰 사람 박스에 잘못 붙는 것을 막는다.
- 사람이 겹친 군중에서는 한 머리가 여러 사람 박스의 후보가 되므로, 1:1 배정을 해서 중복으로 세지 않게 한다.
- `top_ratio`와 크기 비율은 **카메라별로 설정**할 수 있게 한다 (7.1). 카메라 바로 아래처럼 내려다보는 각도가 가파른 영역에서는 머리가 박스 상단이 아니라 가운데 쪽에 찍히기 때문이다.
- 미확정 person과 head의 개수는 로그로 남겨 규칙을 튜닝하는 데 쓴다.
- **알려진 한계**: 원거리에서 머리가 검출되지 않으면 사람 박스도 확정되지 않아 과소 추정될 수 있다. 3.7의 최소 검출 크기로 ROI를 제한해 줄이고, 필요하면 원거리 ROI만 "person 단독 인정"으로 예외를 두는 방안을 검토한다.

### 5.4 구역별 집계

1. 확정 사람 박스의 **하단 중심점(발 위치)** 이 속한 ROI로 구역을 배정한다.
2. 프레임별 구역 인원을 구하고, 5장의 **중앙값**을 그 주기의 인원 `c_t`로 한다.
3. 신뢰도는 5장의 편차 `d = max - min`으로 구한다. 인원이 적을 때는 비율이 쉽게 커지므로 **최소 허용치(절대값)** 를 같이 둔다.

| 조건 | confidence | 처리 |
|---|---|---|
| d ≤ max(2, 0.2 × median) | `high` | 사용 |
| d ≤ max(4, 0.5 × median) | `medium` | 사용 |
| 그 외 | `low` | **주기 폐기**. 직전 `level`/`score`를 `confidence: low`로 다시 기록하되 `updated_at`은 그대로 둔다 |

(임계값은 잠정)

- 비율만 쓰면 빈 구역(예: 0,0,1,0,2명)이 계속 `low`로 폐기되어, 사람이 빠져나간 뒤에도 예전 혼잡 값이 남는다. 최소 허용치는 이것을 막기 위한 것이다.
- `low`가 3주기 연속되면 `updated_at`이 90초를 넘겨 웹에서 `stale`로 보인다. 측정이 불안정하다는 사실이 화면에 드러나도록 의도한 동작이다.

### 5.5 스무딩과 점수

```
EMA:   s_t = α · c_t + (1 - α) · s_{t-1},  α = 0.4 (잠정, 최근 3~5주기 반영)
score: score_t = clip(s_t / capacity_zone, 0, 1)
```

- `capacity_zone`은 구역별 캘리브레이션 값이다 (7.2). 구역 면적과 시야가 다르므로 공통값을 쓰지 않는다.
- 초기값: 시작 후 첫 유효 주기에서 `s_1 = c_1`. 폐기된 주기에서는 EMA를 갱신하지 않는다.

### 5.6 3단계 판정 (히스테리시스)

| 전이 | 조건 (잠정) |
|---|---|
| 0 → 1 | score ≥ 0.45 |
| 1 → 2 | score ≥ 0.75 |
| 2 → 1 | score < 0.65 |
| 1 → 0 | score < 0.35 |

- 올라갈 때는 상승 임계값, 내려갈 때는 하강 임계값만 본다. 한 번에 두 단계를 건너뛸 수 있다 (예: 0단계에서 score ≥ 0.75면 바로 2단계).
- 단계 변경은 **2주기 연속** 조건을 만족할 때만 반영한다 (잠정).
- 반응 지연: EMA와 2주기 확인 때문에 급격한 혼잡이 화면에 반영되기까지 약 1~1.5분 걸린다. 너무 느리면 α를 올리거나 연속 조건을 1주기로 줄인다.

### 5.7 블러

| 항목 | 설계 |
|---|---|
| 대상 | 5장 중 인원 수가 중앙값인 프레임 1장 |
| 방식 | 전체 균일 블러: 긴 변 기준 1/8로 다운스케일 → 가우시안 블러 → 표시 크기로 업스케일 (잠정) |
| 검증 | ① 다운스케일 단계의 해상도가 목표 이하인지 확인 ② 결과 이미지의 선명도(라플라시안 분산)가 임계값 이하인지 확인 ③ 블러 처리 중 예외가 나면 실패 |
| 실패 시 | **이미지 미전송**, 수치만 전송 (`image_url: null`) |
| 출력 | JPEG. 전달 방식은 5.8 참고 |

검증에 검출 모델을 다시 돌리지 않는다. 강한 블러를 거친 이미지에서는 모델이 머리를 거의 못 찾으므로 실패를 잡아내지 못하고 추론 시간만 늘어나기 때문이다. 대신 "블러가 실제로 적용됐는가"를 직접 확인한다.

카메라 한 대의 이미지를 구역별로 자를지, 카메라 전체 이미지를 공유할지는 웹 화면 설계와 합의해서 정한다.

### 5.8 전송

- 구역마다 JSON을 Redis에 `SET`한다 (9장).
- 이미지 바이트는 JSON에 넣지 않고 **Redis 별도 키**(`ywhob:img:{zone_id}`)에 저장한다. 웹 서버가 이 키를 읽어 `/snapshots/{zone_id}.jpg`로 제공한다 (잠정, 웹 담당과 협의).
  - 노트북과 웹 서버가 서로 다른 장비라서, 노트북 디스크에 쓴 파일은 웹 서버가 읽을 수 없다. 그래서 이미 공유하는 Redis로 이미지를 넘긴다.
  - 이미지를 먼저 쓰고 JSON을 나중에 쓴다. 그래야 JSON이 아직 없는 이미지를 가리키는 일이 없다.
- `image_url`에 버전 쿼리(`?v={unix_ts}`)를 붙여 브라우저가 옛 이미지를 캐시에서 보여주지 않게 한다.
- Redis 연결이 실패하면 재시도하고, 데이터를 쌓아두지 않고 최신 값만 유지한다.

---

## 6. 동시성 구조

```
[capture worker ×N] ──frame queue──▶ [inference worker ×1] ──result queue──▶ [aggregate/blur/publish worker]
```

| 요소 | 방식 |
|---|---|
| 캡처 | 카메라별 스레드 (I/O 대기 위주) |
| 추론 | 단일 프로세스, 배치 추론 (GPU 또는 CPU 독점) |
| 후처리 | 별도 스레드 또는 프로세스 |
| 큐 | 크기 제한. 가득 차면 **가장 오래된 주기를 버림** (지연보다 최신성 우선) |
| 감시 | 주기마다 처리 시간을 로깅하고, 30초를 넘으면 경고 |

---

## 7. 구역 설계와 캘리브레이션

### 7.1 설정 파일

`configs/cameras.yaml` (예시)

```yaml
cameras:
  - id: cam01
    url: rtsp://<host>/stream1
    zones:
      - zone_id: hall_a_front
        roi: [[120, 400], [980, 400], [1260, 710], [20, 710]]   # 다각형, 픽셀 좌표
        capacity: 60
        mode: normal
      - zone_id: hall_a_back
        roi: [[300, 180], [900, 180], [980, 400], [120, 400]]
        capacity: 90
        mode: normal
    matching:        # 5.3 사람 확정 규칙. 카메라 각도에 따라 조정
      top_ratio: 0.4
      head_width_ratio: [0.15, 0.7]
    mask:            # 최소 검출 크기 미만 영역 (3.7)
      - [[0, 0], [1280, 0], [1280, 180], [0, 180]]
```

### 7.2 캘리브레이션 절차

1. 카메라를 설치하고 현장 프레임을 수집한다.
2. 근거리와 원거리를 나눠 ROI를 그리고, 최소 검출 크기 미만 영역은 마스킹한다.
3. 물리 구역(부스 배치)과 ROI를 매핑한다.
4. `capacity` 초기값을 정한다: 구역 면적(m²) × 기준 밀도(잠정 1.5명/m²).
5. 행사 전 리허설이나 초기 운영 데이터로 `capacity`와 단계 임계값을 조정한다.

ROI 편집용 간단한 도구(`scripts/roi_editor.py`: 프레임 위에 다각형을 그리고 YAML로 출력)를 둔다.

---

## 8. 개인정보 처리

| 항목 | 명세 |
|---|---|
| 원본 프레임 | 메모리에만 존재. 디스크, 로그, 네트워크 어디에도 기록하지 않음 |
| 디버그 모드 | 원본 저장 옵션을 두지 않음. 필요하면 블러 이미지만 저장 |
| 전송 이미지 | 5.7의 블러와 검증을 통과한 1장만 |
| 학습용 현장 샘플 | 별도 동의와 절차 필요 (주최 측 협의). 라벨링 환경 접근 제한 |
| 서버 보관 | 웹 서버 측 정의 필요 (범위 밖, 미결) |

---

## 9. 인터페이스

### 9.1 Redis 키

| 키 | 값 | 비고 |
|---|---|---|
| `ywhob:zone:{zone_id}` | 구역 JSON | 주기마다 덮어쓰기 |
| `ywhob:img:{zone_id}` | 블러 JPEG 바이트 | 주기마다 덮어쓰기. TTL 120초 (잠정) |
| `ywhob:zones` | 구역 ID 목록 (Set) | 웹 서버의 전체 조회용 |
| `ywhob:heartbeat` | 마지막 파이프라인 동작 시각 | 파이프라인 생존 확인 |

### 9.2 구역 JSON

```json
{
  "zone_id": "hall_a_front",
  "level": 1,
  "score": 0.52,
  "confidence": "high",
  "image_url": "/snapshots/hall_a_front.jpg?v=1789882200",
  "updated_at": "2026-09-20T14:30:00+09:00",
  "status": "ok"
}
```

| 필드 | 타입 | 설명 |
|---|---|---|
| `level` | int | 0/1/2 |
| `score` | float | 0.0~1.0 |
| `confidence` | string | `high` / `medium` / `low` |
| `image_url` | string \| null | 블러 실패 시 `null` |
| `updated_at` | string | ISO 8601, KST |
| `status` | string | `ok` / `stale` / `error` |

**status 판정**
- `error`: 파이프라인이 카메라 장애나 캡처 부족을 감지해 직접 기록한다.
- `stale`: 웹 서버가 `now - updated_at > 90초`일 때 판정한다. 파이프라인이 죽으면 스스로 `stale`을 쓸 수 없기 때문이다.
- 웹 서버는 `stale`이나 `error`를 "여유(0단계)"로 표시하지 않는다.

### 9.3 전달 방식

30초 주기이므로 웹 서버가 폴링한다. 푸시가 필요해지면 SSE를 추가한다.

---

## 10. 운영과 장애 처리

| 상황 | 동작 |
|---|---|
| 카메라 연결 끊김 | 재연결 시도 (지수 백오프), 해당 구역 `status: error` |
| 주기 처리 시간 초과 | 다음 주기를 건너뛰고 경고 로그 |
| 신뢰도 `low` | 주기 폐기, 직전 값을 `confidence: low`로 기록 (`updated_at`은 갱신하지 않음) |
| 블러 실패 | 이미지 미전송, 수치는 전송 |
| Redis 장애 | 재시도, 파이프라인은 계속 동작 |
| 프로세스 종료 | OS 서비스나 스크립트로 자동 재시작 |

**로그**: 주기별 카메라 ID, 처리 시간, 구역별 인원, 미확정 person/head 수, 신뢰도. 이미지는 남기지 않는다.

---

## 11. 테스트와 검증

| 단계 | 방법 |
|---|---|
| 단위 테스트 | 매칭 규칙, 중앙값/신뢰도, EMA, 히스테리시스, 블러 검증 로직 |
| 재생 테스트 | 녹화 영상을 가짜 RTSP로 흘려 전체 파이프라인을 끝까지 실행 |
| 부하 테스트 | 가짜 카메라 N대로 수용 카메라 수 확인 (4.3) |
| 현장 검증 | 리허설 때 수동 계수와 비교해 구역 인원 MAPE 측정 |
| 장애 테스트 | 카메라 차단, Redis 중단 시 `error`/`stale` 표시 확인 |

---

## 12. 디렉터리 구조 (제안)

```
YwHob/
├── README.md
├── spec.md
├── configs/
│   ├── data.yaml           # 학습 데이터셋 정의
│   ├── train.yaml          # 학습 하이퍼파라미터
│   ├── cameras.yaml        # 카메라, ROI, 캘리브레이션
│   └── service.yaml        # 주기, 임계값, Redis 설정
├── data/                   # (gitignore) 원본/변환 데이터
├── models/                 # (gitignore 또는 LFS) 가중치, ONNX, 모델 카드
├── scripts/
│   ├── convert_crowdhuman.py
│   ├── convert_scuthead.py
│   ├── train.py
│   ├── evaluate_count.py   # 인원 MAE/MAPE, 크기별 recall
│   ├── export.py
│   ├── benchmark.py
│   └── roi_editor.py
├── ywhob/
│   ├── capture.py
│   ├── detect.py
│   ├── matching.py         # 사람 확정 규칙
│   ├── aggregate.py        # 중앙값, 신뢰도, EMA, 판정
│   ├── blur.py
│   ├── publish.py
│   └── main.py
└── tests/
```

---

## 13. 진행 단계

| 단계 | 작업 | 산출물 | 선행 조건 |
|---|---|---|---|
| M1 | 개발 환경, 데이터 변환 스크립트 | `data.yaml`, 변환 스크립트 | 없음 |
| M2 | YOLO26s 1차 파인튜닝 (CrowdHuman) | 기준선 모델, 지표 | M1 |
| M3 | 현장 샘플 수집, 라벨링, 추가 학습 | 현장 적응 모델 | 현장 촬영 협의 |
| M4 | 최소 검출 크기 실측, 매칭 규칙 튜닝 | 크기별 recall, 규칙 확정 | M3 |
| M5 | ONNX 변환, 양자화, 벤치마크 | 배포 모델, 수용 카메라 수 | M2 이상, 노트북 사양 |
| M6 | 파이프라인 구현 (캡처 → 전송) | `ywhob/` 패키지 | M5 |
| M7 | Redis 연동, 웹 서버 합의 | 인터페이스 확정 | 웹 담당자 협의 |
| M8 | 캘리브레이션, 리허설 검증 | `cameras.yaml`, 검증 결과 | 카메라 설치 |

M1~M2와 M6의 뼈대 구현은 현장 정보 없이 바로 시작할 수 있다.

---

## 14. 미결 사항

| 항목 | 영향 범위 |
|---|---|
| 카메라 대수, 위치, 실제 높이, 스트림 사양 | 5.1, 7장, 4.3 |
| 노트북 사양 (GPU 유무) | 4.1 런타임, 수용 카메라 수 |
| 학습 환경 (GPU 서버, 클라우드 등) | 3장 전체 |
| CrowdHuman, SCUT-HEAD 라이선스의 행사 사용 가능 여부 | 3.3 |
| 이미지 전달 방식 (Redis 별도 키) 웹 담당 합의 | 5.8, 9장 |
| 사람 확정 규칙 세부 (수정 예정) | 5.3, 원거리 과소 추정 |
| 원거리 ROI 예외 규칙 필요 여부 | 5.3, 7장 |
| 블러 강도 기준, 이미지 보관 정책 | 5.7, 8장 |
| 현장 샘플 촬영과 라벨링 허가 | 3.3, M3 |
| 주최 측 개인정보 요구사항 | 8장 |
| 이미지 단위 (구역별 크롭 vs 카메라 전체) | 5.7, 9장 |
